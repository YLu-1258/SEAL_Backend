version: '3'
services:
        web:
                image: seal_backend_v1
                build: .
                ports:
                        - "1337:8086"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped